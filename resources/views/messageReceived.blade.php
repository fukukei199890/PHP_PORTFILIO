<x-user-layout>
    {{-- <ul>
        @foreach (Auth::user()->unreadNotifications as $notification)
            <li>{{ $notification->sender_name }}からのメッセージがあります</li>
        @endforeach
    </ul> --}}
    <h1>a</h1>
</x-user-layout>
